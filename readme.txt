# Spreadsheet in C++
A spreadsheet class written in C++ that carries out some Excel-like tasks:

 
## Build
To run the examples, in a terminal navigate to this directory, then run

mkdir build
cd build
cmake ..
cmake build .


